#include<stdio.h>
void fib(int a,int b,int n)
{
	long long int fl,i,j,k,y;
  
 
  for(fl=1;fl<=n;fl++)
  {
      i=a;           //i is intializing with a
      j=b;          //j is intializing with b
      k=i+j;       //k is sum of i and j
      printf("%llu\n",k);
      a=b;       //now a i intialize with b
      b=k;      //b is initialize with k
  }
}
int main()
{
  long long int fl,a=0,b=1,i,j,k,y;
  printf("Enter range of fibonacci series: ");
  scanf("%llu",&y);
  fib(0,1,y);
 
}
